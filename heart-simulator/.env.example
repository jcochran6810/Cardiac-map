# Optional: enables the Claude-powered AI tutor. Without it the tutor answers
# from the built-in knowledge base (the app works fully offline).
ANTHROPIC_API_KEY=
# Optional: override the tutor model (default claude-opus-5)
TUTOR_MODEL=
